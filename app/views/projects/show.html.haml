- content_for :title, @project.name

- if @project.tasks.empty?
  No tasks
- else
  .stats_summary
    %span.label Average lead time:
    = @project.avg_lead_time
    days
  #table-wrapper
    = render 'table'